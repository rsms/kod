# @title RPM specification
# @matchuti org.rpm.spec
# @matchext spec

include "_script_comment.lang"

keyword = '^[[:word:]-]+:'

include "url.lang"
include "_number.lang"

variable = '\$[[:word:]]+'

preproc = '%(\{?)[[:word:]]+(\}?)'