# @title pkg-config
# @matchuti public.pkg-config-script
# @matchext pc
# @matchname pkgconfig

include "properties.lang"

variable = '\$\{([^[:blank:]]+)\}'

include "_number.lang"
include "_url.lang"

(string,symbol) = `(^[^:]+)(:)`